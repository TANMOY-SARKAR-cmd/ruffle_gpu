name: Build and Release

on:
  push:
    tags:
      - 'v*'

  # Allow for manual dispatch on GitHub
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: ruffle_gpu-linux-x86_64
            build_cmd: |
              sudo apt-get update
              sudo apt-get install -y pkg-config libasound2-dev libudev-dev
              cargo build --release --verbose
              cp target/release/ruffle_desktop ruffle_gpu-linux-x86_64 2>/dev/null || \
              cp target/release/$(ls target/release | grep -E '^ruffle[^.]*$' | head -1) ruffle_gpu-linux-x86_64

          - os: windows-latest
            artifact_name: ruffle_gpu-windows-x86_64.exe
            build_cmd: |
              cargo build --release --verbose
              $binary = Get-ChildItem target/release -Filter "ruffle*.exe" | Select-Object -First 1
              Copy-Item "target/release/$($binary.Name)" -Destination "ruffle_gpu-windows-x86_64.exe"

          - os: macos-latest
            artifact_name: ruffle_gpu-macos-x86_64
            build_cmd: |
              cargo build --release --verbose
              cp target/release/ruffle_desktop ruffle_gpu-macos-x86_64 2>/dev/null || \
              cp target/release/$(ls target/release | grep -E '^ruffle[^.]*$' | head -1) ruffle_gpu-macos-x86_64

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: actions-rust-lang/setup-rust-toolchain@150fca883cd4034361b621bd4e6a9d34e5143606 # v1
        with:
          toolchain: stable

      - name: Cache Rust dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Build (Linux/macOS)
        if: runner.os != 'Windows'
        run: ${{ matrix.build_cmd }}

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: ${{ matrix.build_cmd }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_name }}

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          gh release create "$TAG" \
            --title "Release $TAG" \
            --generate-notes \
            artifacts/**/* || true
